#ifndef COLORRGB_H
#define COLORRGB_H


class ColorRGB
{
    public:
        float r;
        float g;
        float b;

        ColorRGB();
        ColorRGB(float r, float g, float b);
        virtual ~ColorRGB();

    protected:

    private:
};

#endif // COLORRGB_H
